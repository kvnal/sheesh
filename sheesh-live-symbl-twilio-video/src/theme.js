import { createMuiTheme } from '@material-ui/core';

export default createMuiTheme({
    palette: {
        type: 'light',
        primary: {
            main: '#F30059',
        },
        secondary: {
            main: '#F30059'
        },
    },
    sidebarWidth: 260,
    sidebarMobileHeight: 90,
});
